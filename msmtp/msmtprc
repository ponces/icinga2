# Set default values for all following accounts.
defaults

auth           on
tls            off
#tls_trust_file /etc/ssl/certs/ca-certificates.crt
logfile        /var/log/msmtp.log
aliases        /etc/aliases

# Account
account        local
host           localhost
port           25
from           noreply@icinga.com
user           albertoponces@criticalmanufacturing.com
password       none

# Set a default account
account default: local
